#!/usr/bin/env sh
sdk_root="$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)"
lld="$sdk_root/ndk/30.0.16248370/toolchains/llvm/prebuilt/linux-aarch64/bin/lld"
if [ ! -x "$lld" ]; then
    echo "lld: Android NDK 30.0.16248370 is not installed in this fixed SDK." >&2
    exit 1
fi
exec "$lld" "$@"

